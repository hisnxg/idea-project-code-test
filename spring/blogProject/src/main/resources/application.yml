spring:
  datasource:
    url: jdbc:mysql://localhost/blog?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false #关闭thymeleaf缓存

## Mybatis plus 配置
mybatis-plus:
  type-aliases-package: com.java.demo.domain
  mapper-locations:
     classpath: /mapper/*.xml
## 端口
server:
  port: 8081
#日志
logging:
  level:
    com.nxg: debug

